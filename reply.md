两项新增依赖的范围审查完成：**建议按附件限定范围修订后纳入隔离候选；两份现补丁均不宜原样落地，最终由队长核定。** 本轮未应用补丁或执行测试，不构成 P0/P1 验收通过。

- **intake 确实增加产品能力**：它是可创建 issue/run、指定负责人并返回幂等回执的受认证事件入口，不只是测试类型依赖。建议仅纳入已发布流程适配器，明确跨人指派与真实调用身份边界；补齐被忽略的 instance/debug 控制字段及显式版本输入处理。保留 callback 明确拒绝、零副作用及全部排除边界，不引入匿名/签名 webhook。
- **issue 保护方向必要，但当前实现有阻断项**：单项在后续字段/CAS校验前就提交取消；批量“预检”实际逐项取消；批量 no-op 与单项不一致；规范化 key 不等于有效状态，自定义取消状态会被误拒绝。取消前枚举任务且吞掉查询错误，也不能证明并发任务已停止。这些是静态发现，未冒充动态复现。
- **最小修订要求**：纯校验先行，状态判断、issue 更新与工作流取消共享事务和并发保护，复用引擎状态机；提交后通过 TaskService 停止关联任务并支持失败恢复。必要的既有 engine/handler 支持差异须随修订具体列出，不复制状态机或扩大到 Issue Pool。
- 附件列明逐文件边界、权限/状态/取消契约、必要回归和完整 P1 装配门槛。实际 r4 ZIP 摘要及内层68项均匹配；本轮依据未应用补丁与固定上游静态审查，未重做162项盘点、权限抽取或 TES-88/P0复验。

P1 仍需新固定候选的全包 build/vet/真实DB测试、合法身份匹配 CLI 调用及真实 daemon 取消/恢复闭环；现有111项、end-node HTTP与CLI拒绝记录不能替代。TES-85 保持 `in_progress`，真实8条继续归档、`apply_allowed=false`；身份、原生验收和生产切换门槛不变。
