# Ensure shell scripts always use LF line endings (needed for Docker on Windows)
*.sh text eol=lf
docker/entrypoint.sh text eol=lf

# Default behavior
* text=auto

# Immutable Memory snapshots and their exact readable exports are hash-verified.
datas/memory/projects/** -text
